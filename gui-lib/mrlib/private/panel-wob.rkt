#lang racket/base
(require "../panel-wob.rkt")
(provide (all-from-out "../panel-wob.rkt"))
;; this file is for backwards compatibility, as some
;; other libraries might be (inappropriately)
;; using the private version
